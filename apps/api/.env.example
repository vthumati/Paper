# Copy to .env for local overrides. Defaults work out of the box (SQLite).
PAPER_DATABASE_URL=sqlite:///./paper_dev.db
# For Postgres (staging/prod):
# PAPER_DATABASE_URL=postgresql+psycopg://user:pass@host:5432/paper
PAPER_JWT_SECRET=dev-insecure-change-me
